# tools/update

Vendor refresh helpers for ignored local tool integrations.

Keep generated clones, backups, and temporary update state outside tracked source; use project-root `tmp/` or an explicit `TMPDIR`.
