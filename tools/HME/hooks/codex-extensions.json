{
  "description": "Codex-only hook events appended to the canonical hooks.json projection. PermissionRequest is a Codex CLI lifecycle that Claude Code does not emit; all host hook commands route through event_kernel/host_hook_entry.js, keeping tools/HME/hooks as source of truth.",
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/event_kernel/host_hook_entry.js --host codex --event PermissionRequest",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
