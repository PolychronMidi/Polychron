"""Synthesis subpackage — inference providers and session management."""
# Minimal init — individual modules are imported by the parent __init__.py
# and aliased via _alias_subpackage(). Don't import everything here to
# avoid circular import issues during package initialization.
