# request_mutation/

Bounded-context façade: transforms the inbound client request before
dispatch. Public surface re-exported from `proxy/hme_proxy_request_mutation.js`.
See `doc/PROXY_CONTEXTS.md` for the full context contract.
