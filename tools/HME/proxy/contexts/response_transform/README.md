# response_transform/

Bounded-context façade: buffers and rewrites the upstream response
into Anthropic-compatible SSE/JSON. Public surface re-exported from
`proxy/hme_proxy_anthropic_response.js`. See `doc/PROXY_CONTEXTS.md`.
