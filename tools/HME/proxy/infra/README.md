# infra/

Foundational infrastructure for the HME proxy: filesystem paths, config
resolution, subprocess wrappers, and lifecycle state. Anything proxy code
needs before it can route a single byte lives here.
