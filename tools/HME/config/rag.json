{
  "_comment": "HME RAG configuration. Fields: ragIgnore (pathspec glob list for path-specific exclusions like doc/theory/**), ragLibs (external library configs). Walker root = PROJECT_ROOT, with .gitignore + ragIgnore + HME_IGNORE_DIRS basename excludes filtering. Size limit → HME_RAG_MAX_FILE_SIZE_KB in .env.",
  "ragIgnore": ["doc/theory/**", "tools/csv_maestro/**", "src/types/**"]
}
