{
  "$schema": "./roles.schema.json",
  "_doc": "Bounded peer roles. ask-peer.sh is the sole writer for channel transcripts and per-role session files. I3 red/blue/purple mesh RETIRED (Phase-4 trial: did not beat the single-peer baseline on catch-per-cost). Only the proven single-peer loop remains.",
  "roles": {
    "blue_lead": {
      "channel": "teams/driver.md",
      "session_file": "tmp/.team-blue_lead.session",
      "tier": "E5",
      "effort": "high",
      "max_reply_bytes": 12000,
      "label": "Driver peer (single bounded reviewer)"
    }
  }
}
