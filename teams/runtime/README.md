# teams/runtime

Ignored, regenerated team runtime state.

Contents are per-machine/per-session: peer Claude session ids, channel lockfiles,
tail-cap temp files, and round output. `ask-peer.sh` and the round runners
recreate these files as needed. Do not hand-author durable design or evidence
here; use `teams/capsules/` or `teams/rounds/` instead.
