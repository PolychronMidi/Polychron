# composers/utils/

Composer-local helpers shared across the per-style composer modules
(BluesComposer, ChromaticComposer, MeasureComposer, etc.). Scoped to
composition concerns -- truly project-wide bootstrap utilities live in
`src/utils/` (validator, moduleLifecycle, feedbackRegistry).
