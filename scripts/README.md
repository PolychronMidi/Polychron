# scripts

Root audit-wrapper entrypoints expected by HCI verifiers.

Each wrapper delegates to the canonical implementation under `tools/HME/scripts/` while preserving the historical root `scripts/` interface.
