const fs=require('fs');const s=fs.readFileSync('./src/writer.js','utf8');const lines=s.split(/\r?\n/);let bal=0;const out=[];for(let i=0;i<lines.length;i++){const o=(lines[i].match(/\{/g)||[]).length;const c=(lines[i].match(/\}/g)||[]).length;bal+=o-c;out.push({line:i+1,bal,code:lines[i]});}fs.writeFileSync('./output/brace_balance.json',JSON.stringify(out,null,2));console.log('Wrote output/brace_balance.json');
