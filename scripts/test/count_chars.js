const fs=require('fs');const s=fs.readFileSync('./src/writer.js','utf8');const bt=(s.match(/`/g)||[]).length;console.log('backticks',bt);const openPar=(s.match(/\(/g)||[]).length,closePar=(s.match(/\)/g)||[]).length;console.log('parens open',openPar,'close',closePar);const openBr=(s.match(/\{/g)||[]).length,closeBr=(s.match(/\}/g)||[]).length;console.log('braces',openBr,closeBr);const lines=s.split(/\r?\n/);console.log('total lines',lines.length);console.log('preview around 140-170:');console.log(lines.slice(140,170).join('\n'));
