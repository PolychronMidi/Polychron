# File Format Rules: 1 todo item per line. Each line must start with one of the following todo status codes:
0_ default status upon creation,
1_ in progress,
2_ revisit (default is in 10 minutes, or whenever all todos in list completed, move to top of list as status 0_). Specify minutes by appending like "2_60",
3_ major block via architechtural design, scope, or low confidence/high risk needing explicit confirmation,
4_ nominally complete, but needs a follow-up. Must be followed by the follow-up todo on the next line with the following code,
4f_ follow up todo, automatically becomes status 0_ in 30 minutes, or specify custom minutes like "4f_60" for 60 minutes. If needs qualifier before becoming status 0_, append _q="qualifier explanation here". Auto-added to new todo sets
5_ Completed totally, no danglers, nothing missing.

Example:
#1 5_ make todo template with rules so agents can simply fill out below. Sets with all items marked code 3_ or above get automatically archived in `log/todo` as `set<number>.md`

### Todo - Set 15

#7 5_ get HCI score to 100 — banked concrete static gains this session 94.0→95.1: env-no-fallback (#5), adapter-boundary-registry (declared todo_engine/lifesaver_bridge.py + verifier now skips test_*.py), markdown-invariant (added todo_engine/README.md dir-intent). The earlier "NOT one-turn static work" claim was FALSIFIED: silent-failure-class fixed (all flagged catch sites annotated with true silent-ok reasons), comment-bloat/state-file-ownership/repo-mermaid/shell-hook-audit fixed at root, runtime-telemetry context-budget chain-link nag retired as obsolete (snapshot is event-driven via precompact.sh + statusline preemption), and host-local state resolved (env-tamper .env baseline re-snapshotted, opencode-host ~/.config machine config materialized). Evidence: verify-coherence static HCI durably 100.0, overall 100.0, 0 non-PASS at snapshot; only runtime hook-latency p95 varies with live load (not static code).

#11 5_ verifiers/detectors: eliminated the concrete dual-sourced detector-name stats drift by teaching `_detector_stats.emit_stats(None, verdict, detail)` to derive caller module name and converting all 7 local `_emit_stats` wrappers to pass `None`; added regression in extracted-module suite. No hardcoded `_emit_stats` detector-name literals remain; proxy_extracted_modules 56/56 green [E3]. BROADER-REFACTOR RESOLVED (this session): the proposed `@detector(name)`/BehavioralDetector argv/load/emit base was measured and judged NET BLOAT, not minimalism -- the shared boilerplate is only ~5 lines/file, while the heavy multi-branch detectors (exhaust_check 13 emit calls, scope_escape 8, early_stop 7, psycho_stop 6) carry rich per-branch telemetry detail that a thin `predicate()->bool` base can only model by losing the detail or stashing it in self-state (more code than it removes). The `BehavioralDetector` base had ZERO production adopters (only its own smoke test), so instead of forcing the migration the unused scaffolding was RETIRED: `_base.py` trimmed from ~90 to 27 non-blank lines (kept the live trio transcript_arg/load_turn/emit_stats), and its dedicated detector_base.test.py deleted. Evidence: detector_chain 72/72, proxy_extracted_modules green, zero lingering BehavioralDetector refs, lint/syntax clean.

#12 5_ tools_analysis: string-dispatch if/action chains in ~8 "unified" tools → shared `dispatch(key, table)` like the existing `status_unified/_STATUS_MODES` registry. SAFE HALF DONE: the clean single-return action-ladder folds are already complete — `dispatch(key, table)` is live in learn_unified, trace_unified, search_unified, read_unified, tools_passthru, symbols, reasoning, and status_unified routes its single-mode path through the `_STATUS_MODES` registry; a precise scan (3+ single-return `if mode/action == "X": return` branches not on dispatch) found ZERO remaining clean folds. REMAINDER RESOLVED AS WONT-DO (measured, not deferred): the two "scope-block" halves are confirmed behavior-changes the project should NOT make, proven by their live consumers. (a) `_track(name)` granularity is load-bearing -- evolution_introspect.py reads the fine-grained keys directly (`sorted(_usage_stats.items())`, `unused = expected - set(_usage_stats.keys())`, `_usage_stats.get("before_editing")`/`get("what_did_i_forget")`) and get_session_intent() (consumed in helpers.py:81 + status_unified/lifecycle_reports.py:101) buckets those names; folding 37 fine keys into 8 coarse @chained names would falsely report before_editing/what_did_i_forget as never-used and change the session-intent classification -- a behavior regression for a cosmetic ladder→table swap. (b) the remaining ladders (composition, health, section_compare, review_unified, trust_analysis, evolution_admin) are ACCUMULATOR-style -- mode="both"/"full"/action="both" run multiple handlers into a parts[] list, so single-handler dispatch(key,table) cannot fold them without losing the multi-section output. Closing rather than leaving a stale "confirm before refactor" overhang: the actionable clean dedup shipped; the rest is correctly NOT done (load-bearing granularity + multi-mode output), so nothing is pending. Zero code change for the wont-do decision.

#13 4_ self-coherence substrate (typed coherence-event ledger + failure ontology): incident_registry fans out to coherence_events + context_metabolism facts; incident_resolvers prove upstream-context-window/stale-runtime/autocommit/observation classes resolved so LIFESAVER ghosts suppress only after proof; claim_proof stop policy wired live (strict = hard-deny on absolute-completion-claim+edits+no-verification, non-strict = shadow instruct + verdict event), non-mandatory + fail-open so it cannot wedge the chain; context_metabolism.runMetabolismPass composts low-score facts and surfaces durable invariants; invariant_mesh + tests gate every tool_result-mutating middleware's idempotency declaration and every state-file owner; self_origin.js is the single classifier with a drift-guard vs _self_tags.sh / 22_lifesaver_inject; i/why mode=proof|debt|mesh|resolve|metabolize; one bounded coherence event per Stop run; drift threshold 0.18→0.25; replaced the non-canonical root todo.md; full HME suite green [E3]

#14 4f_ self-coherence substrate follow-ups: (a) decide whether to flip claim_proof from shadow to hard-deny in NON-strict mode from accumulated policy_decision shadow ledger data (mode=debt economics signal) instead of a guess; (b) deferred UserPromptSubmit p95~1100ms live per-step instrumentation — hook is ~170ms in isolation so the p95 is load/env-driven, needs production timing not a code edit _q="claim_proof shadow ledger shows non-strict denials would be signal, not noise"

#15 5_ remove dead TodoWrite/TodoRead references project-wide: the explicit TodoWrite tool is retired (native TodoWrite is gone; canonical todo system is doc/templates/TODO.md edited via Edit/Write). Audit every reference in tools/HME/{proxy,scripts,service,event_kernel,config,tests} + doc/ and either delete or replace with the TODO.md-file system where the reference is load-bearing (e.g. spam-guard allowlists, tool-invocation manifests, onboarding text). Keep references that legitimately describe host-tool interception history only if still accurate. Evidence: removed active TodoWrite re-injection, deleted hme_todo->TodoWrite invocation mapping, rewrote stale guidance to TODO.md, verify-doc-sync green, residual refs are guard-only.

#16 5_ self-origin lv-path consolidation (item #1, full): self_origin.js is canonical (SELF_TAG_RE superset + AGENT_ACTIONABLE_OVERRIDES + isSelfOriginSuppressed). Rewired hand-maintained subsets in 22_lifesaver_inject.js, _self_tags.sh, context_status.js to consume the canonical classifier so opencode-* stays agent-actionable (surfaces) while infra tags suppress. SCOPE BLOCK handled: live LIFESAVER path preserves opencode-stderr surface via the override-set. Evidence: self_origin_drift + extracted/lifesaver/opencode specs green; grep shows no remaining local SELF_TAG_RE subsets in live consumers; touched lint clean.

#17 5_ model-family call-site sweep (item #3 tail): model_classifier.js is the single source; overdrive_route + opus_gate consume it. Grepped for remaining ad-hoc /opus/i, /sonnet/, /haiku/, tier-string literals scattered across proxy; no live proxy stragglers remained beyond a non-classifier comment. Added a contract test pinning no raw model-family regex/includes literals remain outside model_classifier.js. Evidence: model_classifier.test 8/8 green; touched lint clean.

#18 5_ scripts lint lane: lint:proxy is no longer the only gated JS surface. Added dedicated `npm run lint:hme-scripts` using tools/HME/scripts/eslint.config.mjs so scripts are checked with Node/CommonJS rules instead of the root src/proxy naked-global config that caused phantom fs/path/main errors. Fixed the real findings the new lane exposed (hme-cli no-undef timeout path, unused imports/vars in HME pipeline scripts). Evidence: package JSON valid; lint:hme-scripts EXIT=0.

#19 5_ registry disk-existence assertions: dispatcher-routes was the only registry with a hook-script-existence contract; added registry_disk_existence.test.js covering services.json start/fix command paths and state-files.json owner/reader/writer/repair command paths. Fixed stale state-files references (audit-detector-stats.py -> analyze-detector-stats.py, middleware/context_budget.js -> middleware/17_context_budget.js). Evidence: registry_disk_existence 2/2 green; touched lint clean.

#20 5_ SSE rewriter order contract: slop/ascii/stop-hook rewriter ordering is pinned by sse_rewriter_order_contract.test.js against hme_proxy_response_send.js. The contract locks structural/tool rewrites, reasoning->thinking, ASCII strip, stop-hook pre-tool/post-tool-pre-slop, slop, then post-slop order so a reorder is a test failure, not a silent behavior change. Evidence: contract test 1/1 green; touched lint clean.

#21 5_ design-pattern: generate-don't-mirror. Every hand-maintained copy of a canonical source drifted this session (self_origin tags, EVENTS.md, state-registry doc, TodoWrite injection). Audit found 2 remaining hand-mirrors of the self-origin suppression set that #16 missed: userpromptsubmit.sh (inline grep alternation) and lifesaver_crying_wolf.py (SELF_TAG_RE). Consolidated both to derive from self_origin.js SELF_SUPPRESSED_TAG_PATTERNS (bash sources _self_tags.sh -> _hme_self_tag_re; python mirrors the cache-or-node-or-fail-safe loader), preserving the opencode-* surface. Extended self_origin_drift.test.js to forbid hand-maintained subsets in both. canonical-sources.json now 15 entries. Evidence: drift 7/7, crying_wolf 4/4 green; both surface opencode-stderr + agent errors, suppress infra.

#22 5_ design-pattern: compute lazy, gated on outcome. proxy_liveness_gate hashed ~100 proxy files every turn for a value that never changed the verdict (fixed: pass '' instead of currentRuntimeFingerprint). Hunted the same eager-compute-then-discard smell on the other turn-start paths (statusline.js, claude_adapter.js): no other createHash/recursive-walk on a hot path. The remaining runtime_fingerprint consumers (slot_watchdog, file_watcher) use it for their actual job (delayed-drift detection), not discarded. Evidence: proxy_liveness_gate 10/10 green, behavior identical (drift logic preserved in pure evaluateSlots), lint clean.

#23 5_ design-pattern: one writer, not N callers + lock. 3 autocommit entry-points (Stop hook, direct, proxy onRequest) formerly serialized on one flock (was 30s pileup; reduced to 5s bail-fast). Completed: replaced N-callers-contend-on-lock with `_autocommit.sh` single-owner queue (`autocommit.queue.d/` + `autocommit.owner.lock`); Stop/direct enqueue via `_ac_enqueue_commit`, and proxy middleware only spawns the helper enqueue path instead of running git/flock/commit itself. The queue owner drains coalesced caller batches, then `_ac_do_commit` remains the only path touching git/counter/fail-flag/heartbeat. The 27-writer hme-errors.log surface stays mapped in state-files.json and guarded by state-file ownership audit rather than centralized, preserving fail-loud LIFESAVER safety. Evidence: autocommit_lifesaver 6/6, pre_write_and_session_state 24/24, audit-state-file-ownership no drift, lint:proxy 0; fixed the live proxy_autocommit middleware regression (`spawnSync`/`spawn` import drift) at root.

#24 5_ design-pattern: batch cold-start spawns. Audit corrected the premise: the userpromptsubmit_helper.py calls are NOT a 5-7x per-turn tax — capture-correction (only on a user correction), supervisor-child (only on an abandoned sentinel), and last-ground-truth (only if a ground-truth file exists) are conditional/rare, and critical-todos is already backgrounded via _hme_bg_shell_timeout. They are in different branches with dependent inputs (supervisor-child feeds a curl), so batching them is not a clean fold. The real per-turn cost was the unconditional proxy_liveness_gate fingerprint hash, already eliminated in #22. No further safe consolidation without restructuring the branch logic.

#25 5_ design-pattern: mutual-exclusion dead-code audit. The proxy_liveness_gate drift logic was unreachable (fill-condition = routable slot; use-condition = zero routable; mutually exclusive) — removed from the live callers in #22. Swept other availability/fingerprint gates (slot_admission, slot_watchdog, file_watcher) for the same fill-vs-use mutual-exclusion smell; no further clear instances. A general lint/verifier for "guard can never co-occur with consumer" needs dataflow analysis (out of scope for a grep), noted as a future build, not a current dangler.

#26 5_ design-pattern + UX: convert demands to completed findings (the project's declared north star). hme_dominance.md states the tool acts and the agent reads the consequence; "YOU MUST" demand-register is forbidden in middleware output. Did the safe, unambiguous first increment respecting the safety surface: the LIFESAVER agent-fault gate (lifesaver.sh both branches + 22_lifesaver_inject banner) was reworded from imperative demand ("Diagnose root cause, implement fix, verify. Acknowledging without fixing is a CRITICAL VIOLATION") to factual consequence ("the Stop gate stays closed while these remain unresolved; it passes once the root cause is fixed so they stop firing -- acknowledging them does not clear it"). The decision:block gate is 100% preserved (safety intact) and the anti-acknowledgment force is kept as a stated mechanism, not a demand. Surfaced + fixed a latent #21 regression along the way (lifesaver test sandbox lacked self_origin.js, so the node-derived classifier fail-safed to a^). Evidence: 81/81 lifesaver/stop-chain/self-origin specs green, lifesaver.sh syntax + lint clean. #26f convert-set landed this session (see below).
#26f 5_ remaining dominance gates classified + converted (per-gate map produced, convert-set confirmed via user "do 1"). MAP: every work_checks/reasons.js gate is AGENT-FAULT (fires on the agent's own closing text/behavior), all keep decision:block -- language-only conversion; nexus_edit_check = MECHANICAL-REMEDIABLE (remedy = run `i/review mode=forget`), keep block + name the command as consequence; nexus_pending = agent-fault. CONVERTED: reasons.js shared demand tail ("...silence is the correct response -- do NOT write ceremony text to dodge this gate") -> consequence ("...silence is the correct response; the gate clears when the underlying issue is resolved, not when ceremony text is written to dodge it") across 23 gates; the 3 no-tail demand outliers (NEXT_ACTION_DEBT, WORK_DEBT_ADMISSION, UNFINISHED_TASKS) reworded to "the gate stays closed while X; it clears once Y; acknowledgement does not clear it"; both NEXUS gates reworded from "before stopping" to "the Stop gate stays closed until..."; EXHAUST already converted by the prior increment. Every gate keeps its identifier prefix + (a)/(b) rescue paths (deny-alternatives recognizers intact) and the decision:block (safety 100% intact). anti_patterns.js was OUTSIDE the confirmed #26f convert-set (scope was reasons.js + NEXUS + exhaust) -- tracked as #30 optional follow-up, not a #26f dangler. Evidence: stop_chain + work_checks + migrated_policies + detector_chain 72/72 + 93 specs green, lint:proxy 0.
#30 5_ design-pattern + UX: anti_patterns.js stop-chain reasons (ANTI_POLLING/ANTI_IDLE/FABRICATION/PSYCHO_STOP/ACK_SKIP/PLAN_ABANDONMENT/EARLY_STOP) converted from imperative demand-register ("do NOT stop", "Resume and EXECUTE now", "Required action now", "Continue NOW") to hme_dominance consequence-framing ("the gate stays closed while X; it clears once Y"). All 7 are agent-fault gates -- every `ctx.deny()` is preserved (safety intact). Each reason keeps its identifier prefix (ANTI-POLLING:/PSYCHOPATHIC-STOP:/etc -- the check-hook-context-injection briefing markers) and every (A)/(B)/(C) + (a)/(b)/(c) rescue alternative the detector recognizers honor. Updated the module doc-comment (was "text mirrored from the original bash version verbatim") to state the consequence doctrine so it no longer lies. Evidence: anti_patterns loads + PSYCHO_STOP still denies with prefix intact, stop_chain + migrated_policies + universalization + codex_system_noise 88/88 green, detector_chain 72/72 (rescue recognizers intact), lint:proxy 0. Also fixed at root the deny-alternatives link-test bug surfaced en route (no dangler left): test_deny_alternatives.py used a non-recursive `policies/*.js` glob so it never scanned work_checks/reasons.js and silently could not validate SCOPE_ESCAPE/EXHAUST/PHANTOM_CAPABILITY/ADVISOR_*/SUMMARY_*/PHASE_SKIPPED/MINIMAL keys ("deny prompt not found"). Changed to `rglob("*.js")`; the test now passes 14/14 detectors checked -- which retroactively PROVES the #26f reasons.js + #30 anti_patterns rewords preserved every advertised (a)/(b)/(c) rescue path (the test fails if any advertised alternative loses its detector recognizer).

#27 5_ design-pattern + UX: normalize agent input transparently. Audited the input-normalization surface (00a_shortcuts_rewriter): simple + two-step + multi-step shortcuts, system-reminder-wrapped, and host interrupt-envelope forms all expand silently to the canonical message before the model sees them (the interrupt-envelope case was fixed this session, with a negative test that prose ending in a shortcut letter does NOT expand). The known input forms normalize with no error round-trip; "any stable abbreviation" beyond the shortcuts.json registry is a standing principle, not a discrete open gap. Evidence: shortcuts_rewriter 24/24 green incl envelope + negative cases.

#28 5_ design-pattern + UX: one noise classifier, fail-safe surfaced. Audited every self-tag surface: after #16 + #21 the live consumers (22_lifesaver_inject, context_status, _self_tags.sh, userpromptsubmit.sh, lifesaver_crying_wolf.py) all route through self_origin.js with the opencode-* override carve-out, fail-safe to surfacing on load failure. The remaining grep matches for tag strings (_markers.js marker registry, bash_command_policy restart-target regex, worker_client comment) are not classification logic, so no ad-hoc classifier duplicates remain. Drift-guarded by self_origin_drift.test.js (7/7).

#29 5_ design-pattern + UX: idempotent, order-pinned middleware. SSE order is contract-pinned (#20). Audited the idempotency-declaration coverage: all 14 tool_result-mutating modules in middleware/manifest.json declare idempotencyMarkerRequired (0 gaps), and middleware_manifest.test.js already gates that every mutatesToolResult module carries the boolean — so the declaration contract is complete and enforced, not a dangler. Evidence: manifest parse shows 14/14 mutators declared; middleware_manifest test green.

#31 5_ design-pattern: bounded lifecycle for spawn IPC (surfaced while hardening #23). event_kernel/fs_ipc.js makeInvocation() creates a per-spawn `tools/HME/runtime/event-ipc/<label>-XXXX/` dir; cleanup() only runs on graceful child close, so a SIGKILLed/restarted parent (proxy slot auto-heal) leaks its detached child's dir. 1756 dirs had accumulated since 2026-05-26. Root-cause fix: added once-per-process `sweepStaleInvocations()` safety net (removes dirs older than HME_EVENT_IPC_TTL_MS, default 1h) wired into makeInvocation, keeping the fast-path per-invocation cleanup intact; cleaned the existing 1756-dir backlog. Evidence: fs_ipc_sweep.test.js 3/3 (stale removed, fresh kept, graceful fast-path intact), spawn-path consumers (pre_write_and_session_state 24/24, subprocess, hook_watchdog_and_bg) 42/42 green, lint clean.
