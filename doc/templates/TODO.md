# File Format Rules: 1 todo item per line. Each line must start with one of the following todo status codes:
0_ default status upon creation,
1_ in progress,
2_ revisit (default is in 10 minutes, or whenever all todos in list completed, move to top of list as status 0_). Specify minutes by appending like "2_60",
3_ major block via architechtural design, scope, or low confidence/high risk needing explicit confirmation,
4_ nominally complete, but needs a follow-up. Must be followed by the follow-up todo on the next line with the following code,
4f_ follow up todo, automatically becomes status 0_ in 30 minutes, or specify custom minutes like "4f_60" for 60 minutes. If needs qualifier before becoming status 0_, append _q="qualifier explanation here". Auto-added to new todo sets
5_ Completed totally, no danglers, nothing missing.

Example:
#1 5_ make todo template with rules so agents can simply fill out below. A set auto-archives to `log/todo/set<number>.md` once no item is still in progress (none at 0_/1_/2_) and at least one item is 5_; the non-5_ items (3_/4_/4f_) carry forward into the next set with their codes preserved

### Todo - Set 27

#23 5_ Execute Phase 14 Workstream 1 via mesh -- DONE: corrected round contract is binding for remaining first-class tracked runner(s): typed durable progress_result records (target/rc/reply_bytes/error_log), named-role targets, per-peer reply capture, stale-output cleanup, no destructive canonical channel/session reset; regressions added in round_status.test.py and round_runner_contract.test.py; round_measured fake-run + HCI verified
#24 5_ Execute Phase 14 Workstream 2 via mesh -- DONE: ran live 4-peer claim-audit round on mesh-signal-discipline brief (base+red+red_purple+blue_purple). Accepted + fixed grounded P1s: (a) guard refunded budget on pre-send capsule/context denies (_deny_refund) so a malformed/gap capsule no longer burns a turn without dispatch + regression; (b) round runners labeled driver->lead channel as teams/driver.md (roles.json truth) instead of red/blue; (c) progress_round_finish emits round=failed when any step failed (no false success) + round_status FAILED state + regressions; (d) chained-handoff reply cap raised to 12000 so cross-exam dialogue is not truncated (cost-charter: never abridge peer dialogue). Cap-lint-vs-guard-default downgraded to P2 (#28). Native-Agent-fallback flagged audit-uncertain (#27). HCI + suites verified.
#27 5_ DECLINED (mesh-found audit-uncertain, resolved with contradictory evidence): team_agent_router.py allow+additionalContext when a registry exists but no target matches is NOT a defect to deny -- the existing test 'non-empty registry with no matching tier surfaces helpful diagnostic' enshrines allow+diagnostic as the INTENDED graceful degradation (driver wants an Agent, no crew registered). A hard deny would fail CLOSED on legitimate non-mesh Agent work (cost-control charter violation) and break that test; the bypass is already surfaced (not silent) via additionalContext. Forcing all Agent calls through the mesh is a scope/product decision, not a coherence fix; declined like F1, reversible if the user wants a stricter mesh-owns-Agent gate.
#28 5_ P2 follow-up (mesh-found, downgraded) -- DONE: review_brief.py _CAP=300000 and team_dispatch_guard.py --context-cap default 24000 can disagree; round_measured.sh now derives CTX_CAP from review_brief._CAP (single source of truth) so a brief that lints OK can never be truncated by the guard at dispatch. The guard's conservative 24000 default stays correct for ad-hoc --context-file grounding; first-class brief dispatch always uses the lint cap.
#25 5_ Execute Phase 14 Workstream 3 via mesh -- DONE: ran live 4-peer claim-audit round on a new hci-integrity brief (run_engine aggregation, _base verdict semantics, verifier_self_coverage, skip/waiver config). 4-peer unanimous, zero false positives. Landed all 5 grounded fail-open/misleading P1 fixes with regressions: (a) _preflight_registry fails CLOSED on duplicate verifier names + non-finite/<=0 weights; (b) _normalize_verdict enforces status/score coherence (FAIL can't read 1.0, unknown->ERROR/0, ERROR->0, SKIP pinned 1); (c) verifier_self_coverage no longer exempts itself (now 35 modules, self-test required); (d) unknown waiver entries (waivered-modules) now FAIL. Tests: hci_integrity.test.py (8) + verifier_self_coverage.test.py (+2); snapshot-persist downgraded P2 by peers. HCI 99.1, no FAIL/ERROR.
#26 5_ Execute Phase 14 Workstream 4 -- DONE (continuous discipline, honored across this execution): all work tracked in TODO (#23-#28), peer dialogue stayed in team channels via ask-peer, review framing stayed DATA in review-briefs.json (hci-integrity + mesh-signal-discipline added there, never as docs/capsule files), plan.md Phase 14 status moved to approved/active only under the explicit session goal; no parallel tracker, no docs spillover created.
